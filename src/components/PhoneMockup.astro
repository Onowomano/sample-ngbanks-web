---

import LogoDrawer from "./LogoDrawer.astro";

---

<div class="bc-outer">
  <div class="bc-inner">
    <!-- Status bar -->
    <div class="bc-status">
      <span class="bc-status__time">9:41</span>
      <div class="bc-status__icons">
        <!-- Signal bars -->
        <svg
          width="18"
          height="11"
          viewBox="0 0 18 11"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M10.9004 2.40039C11.1786 2.40039 11.3178 2.40085 11.4336 2.42383C11.9096 2.51851 12.2822 2.89021 12.377 3.36621C12.4 3.48213 12.4004 3.6216 12.4004 3.90039V9.2002C12.4004 9.47887 12.4 9.61848 12.377 9.73438C12.2823 10.2104 11.9096 10.583 11.4336 10.6777C11.3178 10.7007 11.1785 10.7002 10.9004 10.7002C10.622 10.7002 10.482 10.7007 10.3662 10.6777C9.89018 10.583 9.51754 10.2104 9.42285 9.73438C9.39982 9.61848 9.40039 9.47887 9.40039 9.2002V3.90039C9.40039 3.6216 9.39979 3.48213 9.42285 3.36621C9.51755 2.89021 9.8902 2.51851 10.3662 2.42383C10.4821 2.40084 10.6219 2.40039 10.9004 2.40039ZM15.6006 0C15.8788 7.85123e-09 16.018 0.000458564 16.1338 0.0234375C16.6098 0.118122 16.9824 0.489814 17.0771 0.96582C17.1002 1.08174 17.1006 1.22121 17.1006 1.5V9.2002C17.1006 9.47886 17.1002 9.61848 17.0771 9.73438C16.9824 10.2103 16.6097 10.5821 16.1338 10.6768C16.018 10.6997 15.8788 10.7002 15.6006 10.7002C15.3221 10.7002 15.1823 10.6997 15.0664 10.6768C14.5905 10.5821 14.2178 10.2103 14.123 9.73438C14.1 9.61848 14.1006 9.47886 14.1006 9.2002V1.5C14.1006 1.22121 14.1 1.08174 14.123 0.96582C14.2178 0.489814 14.5904 0.118122 15.0664 0.0234375C15.1823 0.000445124 15.3221 0 15.6006 0ZM1.5 6.69922C1.77879 6.69922 1.91826 6.6996 2.03418 6.72266C2.51009 6.81738 2.88184 7.18913 2.97656 7.66504C2.99962 7.78096 3 7.92043 3 8.19922V9.19922C3 9.47801 2.99962 9.61748 2.97656 9.7334C2.88184 10.2093 2.51009 10.5811 2.03418 10.6758C1.91826 10.6988 1.77879 10.6992 1.5 10.6992C1.22121 10.6992 1.08174 10.6988 0.96582 10.6758C0.489906 10.5811 0.118162 10.2093 0.0234375 9.7334C0.000379849 9.61748 0 9.47801 0 9.19922V8.19922C0 7.92043 0.000379849 7.78096 0.0234375 7.66504C0.118162 7.18913 0.489906 6.81738 0.96582 6.72266C1.08174 6.6996 1.22121 6.69922 1.5 6.69922ZM6.2998 4.69922C6.5786 4.69922 6.71807 4.6996 6.83398 4.72266C7.3099 4.81738 7.68164 5.18913 7.77637 5.66504C7.79942 5.78096 7.7998 5.92043 7.7998 6.19922V9.19922C7.7998 9.47801 7.79942 9.61748 7.77637 9.7334C7.68164 10.2093 7.3099 10.5811 6.83398 10.6758C6.71807 10.6988 6.5786 10.6992 6.2998 10.6992C6.02101 10.6992 5.88154 10.6988 5.76562 10.6758C5.28971 10.5811 4.91797 10.2093 4.82324 9.7334C4.80018 9.61748 4.7998 9.47801 4.7998 9.19922V6.19922C4.7998 5.92043 4.80018 5.78096 4.82324 5.66504C4.91797 5.18913 5.28971 4.81738 5.76562 4.72266C5.88154 4.6996 6.02101 4.69922 6.2998 4.69922Z"
            fill="black"></path>
        </svg>

        <!-- Wifi -->
        <svg
          width="16"
          height="12"
          viewBox="0 0 16 12"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            fill-rule="evenodd"
            clip-rule="evenodd"
            d="M7.70042 2.29636C9.93483 2.29646 12.0838 3.15501 13.7032 4.69457C13.8251 4.81343 14.02 4.81193 14.1401 4.69121L15.3058 3.51479C15.3666 3.45356 15.4005 3.37062 15.4 3.28433C15.3995 3.19803 15.3646 3.11549 15.3031 3.05498C11.0528 -1.01833 4.3474 -1.01833 0.0970716 3.05498C0.0355014 3.11545 0.000571881 3.19796 7.20154e-06 3.28426C-0.000567562 3.37055 0.033283 3.45352 0.0940465 3.51479L1.26005 4.69121C1.38006 4.81211 1.57513 4.81361 1.697 4.69457C3.31657 3.15491 5.46579 2.29636 7.70042 2.29636V2.29636ZM7.70042 6.12375C8.92809 6.12367 10.1119 6.57999 11.022 7.40402C11.145 7.52097 11.3389 7.51844 11.4589 7.39831L12.6232 6.22189C12.6845 6.16019 12.7186 6.07648 12.7177 5.98949C12.7168 5.90251 12.6811 5.8195 12.6185 5.75906C9.84735 3.18131 5.55584 3.18131 2.78469 5.75906C2.72209 5.8195 2.68637 5.90255 2.68555 5.98956C2.68473 6.07657 2.71887 6.16028 2.78031 6.22189L3.9443 7.39831C4.06427 7.51844 4.25817 7.52097 4.38125 7.40402C5.29065 6.58053 6.47357 6.12425 7.70042 6.12375V6.12375ZM9.93797 8.93135C10.0002 8.87024 10.0345 8.78614 10.0327 8.69891C10.031 8.61168 9.99327 8.52905 9.92855 8.47053C8.64232 7.38263 6.75852 7.38263 5.47229 8.47053C5.40753 8.529 5.36977 8.6116 5.36792 8.69883C5.36608 8.78606 5.40031 8.87019 5.46254 8.93135L7.4769 10.9639C7.53594 11.0236 7.61643 11.0572 7.70042 11.0572C7.78441 11.0572 7.8649 11.0236 7.92394 10.9639L9.93797 8.93135Z"
            fill="black"></path>
        </svg>

        <!-- Battery -->
        <svg
          width="25"
          height="12"
          viewBox="0 0 25 12"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M18.4102 0C19.6582 0 20.1111 0.130042 20.5674 0.374023C21.0237 0.618053 21.3819 0.976321 21.626 1.43262C21.87 1.88887 22 2.34185 22 3.58984V7.91016C22 9.15815 21.87 9.61113 21.626 10.0674C21.3819 10.5237 21.0237 10.8819 20.5674 11.126C20.1111 11.37 19.6582 11.5 18.4102 11.5H3.58984C2.34185 11.5 1.88887 11.37 1.43262 11.126C0.976321 10.8819 0.618053 10.5237 0.374023 10.0674C0.130042 9.61113 0 9.15815 0 7.91016V3.58984C0 2.34185 0.130042 1.88887 0.374023 1.43262C0.618053 0.976321 0.976321 0.618053 1.43262 0.374023C1.88887 0.130042 2.34185 0 3.58984 0H18.4102ZM3.58984 1C2.60305 1 2.25812 1.06668 1.9043 1.25586C1.62227 1.40669 1.40669 1.62227 1.25586 1.9043C1.06668 2.25812 1 2.60305 1 3.58984V7.91016C1 8.89695 1.06668 9.24188 1.25586 9.5957C1.40669 9.87773 1.62227 10.0933 1.9043 10.2441C2.25812 10.4333 2.60305 10.5 3.58984 10.5H18.4102C19.397 10.5 19.7419 10.4333 20.0957 10.2441C20.3777 10.0933 20.5933 9.87773 20.7441 9.5957C20.9333 9.24188 21 8.89695 21 7.91016V3.58984C21 2.60305 20.9333 2.25812 20.7441 1.9043C20.5933 1.62227 20.3777 1.40669 20.0957 1.25586C19.7419 1.06668 19.397 1 18.4102 1H3.58984ZM23 3.69043C23.0184 3.69985 24.5 4.46155 24.5 5.69043C24.4997 6.92675 23 7.69043 23 7.69043V3.69043Z"
            fill="black"
            fill-opacity="0.36"></path>
          <rect
            x="2"
            y="1.91602"
            width="18"
            height="7.66667"
            rx="1.6"
            fill="black"></rect>
        </svg>
      </div>
      <div class="bc-notch"></div>
    </div>

    <!-- Header -->
    <div class="bc-header">
      <button class="bc-header__back">
        <svg
          width="20"
          height="20"
          viewBox="0 0 20 20"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M12.5 15L7.5 10L12.5 5"
            stroke="black"
            stroke-width="1.66667"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </button>
      <span class="bc-header__title">Choose Bank</span>
      <div class="bc-slot"></div>
    </div>
    <div class="bc-rule"></div>

    <!-- Search -->
    <div class="bc-search">
      <svg
        class="bc-search__icon"
        width="20"
        height="20"
        viewBox="0 0 20 20"
        fill="none"
        aria-hidden="true"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          d="M17.5 17.5L13.9166 13.9166M15.8333 9.16667C15.8333 12.8486 12.8486 15.8333 9.16667 15.8333C5.48477 15.8333 2.5 12.8486 2.5 9.16667C2.5 5.48477 5.48477 2.5 9.16667 2.5C12.8486 2.5 15.8333 5.48477 15.8333 9.16667Z"
          stroke="#04000A"
          stroke-width="1.66667"
          stroke-linecap="round"
          stroke-linejoin="round"></path>
      </svg>

      <input
        id="bc-search"
        type="text"
        placeholder="Search"
        autocomplete="off"
        spellcheck="false"
        aria-label="Search banks"
      />
    </div>
    <div class="bc-rule"></div>

    <!-- Bank list -->
    <div
      class="bc-list"
      id="bc-list"
      role="list"
      aria-live="polite"
      aria-label="Bank list"
    >
    </div>
    <div class="bc-indicator"></div>
    <LogoDrawer variant="sheet" source="phone" />
  </div>
</div>

<script>
  import { BANKS_CDN_URL } from "../lib/constants";
  import type { Bank, BankSelectDetail, LogoStyle } from "../types/bank";

  const list = document.getElementById("bc-list")!;
  const input = document.getElementById("bc-search") as HTMLInputElement;
  const statusTime = document.querySelector(".bc-status__time");

  function updateStatusTime() {
    if (!statusTime) return;

    const now = new Date();
    const hours = now.getHours();
    const minutes = String(now.getMinutes()).padStart(2, "0");

    statusTime.textContent = `${hours}:${minutes}`;
  }

  updateStatusTime();
  setInterval(updateStatusTime, 1000);

  let banks: Bank[] = [];
  let activeCategories: Set<string> | null = null; // null = all
  let currentStyle: LogoStyle = "circle";

  function escapeAttr(value: string): string {
    return value
      .replace(/&/g, "&amp;")
      .replace(/"/g, "&quot;")
      .replace(/'/g, "&#39;");
  }

  function rowAttrs(b: Bank): string {
    return `data-name="${escapeAttr(b.name)}" data-png="${escapeAttr(b.logos.png)}" data-svg="${escapeAttr(b.logos.svg)}" data-category="${escapeAttr(b.category)}"`;
  }

  function bankFromRow(row: HTMLElement): Bank {
    return {
      name: row.dataset.name!,
      category: row.dataset.category ?? "",
      aliases: [],
      logos: {
        png: row.dataset.png!,
        svg: row.dataset.svg!,
      },
    };
  }

  function dispatchBankSelect(bank: Bank) {
    const detail: BankSelectDetail = {
      bank,
      style: currentStyle,
      source: "phone",
    };
    window.dispatchEvent(new CustomEvent("bank:select", { detail }));
  }

  const BATCH = 50;
  const PLACEHOLDER = "/_default/circle.svg";
  let swapEpoch = 0;

  function logoFor(b: Bank): string {
    return b.logos.png;
  }

  function renderSkeletons(n = 10) {
    list.innerHTML = Array.from(
      { length: n },
      () => `
      <div class="bc-row bc-row--skeleton" aria-hidden="true">
        <div class="bc-logo bc-skel"></div>
        <div class="bc-skel bc-skel--name"></div>
      </div>
    `,
    ).join("");
  }

  function row(b: Bank, id = "") {
    return `
      <div class="bc-row" role="listitem" ${rowAttrs(b)}>
        <div class="bc-logo">
          <img${id ? ` id="${id}"` : ""} src="${logoFor(b)}" alt="${escapeAttr(b.name)}">
        </div>
        <span class="bc-row__name">${b.name}</span>
      </div>`;
  }

  function preload(src: string): Promise<void> {
    return new Promise((resolve) => {
      const img = new Image();
      img.onload = img.onerror = () => resolve();
      img.src = src;
    });
  }

  function swapLogo(id: string, src: string, epoch: number) {
    const img = new Image();
    img.onload = () => {
      if (swapEpoch !== epoch) return;
      const el = document.getElementById(id) as HTMLImageElement | null;
      if (el) el.src = src;
    };
    img.src = src;
  }

  function renderAll(data: Bank[]) {
    swapEpoch++;
    const epoch = swapEpoch;
    if (!data.length) {
      list.innerHTML = '<p class="bc-empty">No banks found</p>';
      return;
    }
    const placeholder = `/_default/${currentStyle}.svg`;
    list.innerHTML = data
      .map(
        (b, i) => `
      <div class="bc-row" role="listitem" ${rowAttrs(b)}>
        <div class="bc-logo">
          <img id="ra-${i}" src="${placeholder}" alt="${escapeAttr(b.name)}">
        </div>
        <span class="bc-row__name">${b.name}</span>
      </div>`,
      )
      .join("");
    data.forEach((b, i) => swapLogo(`ra-${i}`, logoFor(b), epoch));
  }

  function getFiltered(): Bank[] {
    const q = input.value.toLowerCase().trim();
    let data = banks;
    if (activeCategories !== null) {
      data = data.filter((b) => activeCategories!.has(b.category));
    }
    if (q) {
      data = data.filter(
        (b) =>
          b.name.toLowerCase().includes(q) ||
          b.aliases.some((a) => a.toLowerCase().includes(q)),
      );
    }
    return data;
  }

  async function init() {
    renderSkeletons();
    const res = await fetch(BANKS_CDN_URL);
    banks = (await res.json()).banks;

    const first = banks.slice(0, BATCH);
    const rest = banks.slice(BATCH);

    await Promise.all(first.map((b) => preload(logoFor(b))));

    swapEpoch++;
    const epoch = swapEpoch;

    list.innerHTML = first.map((b) => row(b)).join("");

    if (rest.length) {
      list.insertAdjacentHTML(
        "beforeend",
        rest
          .map(
            (b, i) => `
          <div class="bc-row" role="listitem" ${rowAttrs(b)}>
            <div class="bc-logo">
              <img id="bl-${BATCH + i}" src="${PLACEHOLDER}" alt="${escapeAttr(b.name)}">
            </div>
            <span class="bc-row__name">${b.name}</span>
          </div>`,
          )
          .join(""),
      );
      rest.forEach((b, i) => swapLogo(`bl-${BATCH + i}`, logoFor(b), epoch));
    }
  }

  input.addEventListener("input", () => {
    renderAll(getFiltered());
  });

  window.addEventListener("ff:filter", (e) => {
    const { category } = (e as CustomEvent<{ category: string }>).detail;
    activeCategories = category === "all" ? null : new Set([category]);
    renderSkeletons();
    setTimeout(() => renderAll(getFiltered()), 10);
  });

  window.addEventListener("ff:style", (e) => {
    const { style } = (e as CustomEvent<{ style: string }>).detail;
    currentStyle = style;
    list.dataset.style = style;
    renderSkeletons();
    setTimeout(() => renderAll(getFiltered()), 10);
  });

  list.addEventListener("click", (e) => {
    const rowEl = (e.target as Element).closest<HTMLElement>(".bc-row[data-name]");
    if (!rowEl || rowEl.classList.contains("bc-row--skeleton")) return;
    dispatchBankSelect(bankFromRow(rowEl));
  });

  list.dataset.style = "circle";
  init();
</script>

<style is:global>
  /* ── Outer frame ──────────────────────────── */
  .bc-outer {
    width: 100%;
    height: 812px;
    background: white;
    border: 1.5px solid #eaeaea;
    border-radius: 56px;
    padding: 20px;
    box-sizing: border-box;
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
    flex-shrink: 0;
  }

  /* ── Inner frame ──────────────────────────── */
  .bc-inner {
    position: relative;
    height: 100%;
    border: 1.5px solid #eaeaea;
    border-radius: 34px;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  /* ── Status bar ───────────────────────────── */
  .bc-status {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    height: 44px;
    flex-shrink: 0;
  }

  .bc-status__time {
    font-size: 13px;
    font-weight: 500;
    color: #030712;
    letter-spacing: -0.02em;
  }

  .bc-status__icons {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .bc-notch {
    position: absolute;
    margin-top: 2px;
    left: 50%;
    transform: translateX(-50%);
    width: 84px;
    height: 20px;
    border: solid 1.5px #eaeaea;
    border-radius: 9999px;
  }

  /* ── Header ───────────────────────────────── */
  .bc-header {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 16px 12px;
    height: 56px;
    flex-shrink: 0;
    border-bottom: 1.5px solid #eaeaea;
  }

  .bc-header__back {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #fff;
    border: 1.5px solid #eaeaea;
    padding: 6px;
    height: 32px;
    width: 32px;
    cursor: pointer;
    border-radius: 999px;
  }

  .bc-slot {
    width: 32px;
    height: 32px;
    flex-shrink: 0;
  }

  .bc-header__back:hover circle {
    color: #d1d5de;
  }

  .bc-header__title {
    flex: 1;
    font-size: 16px;
    font-weight: 500;
    color: #030712;
    letter-spacing: -0.02em;
    text-align: center;
  }

  /* ── Search bar ───────────────────────────── */
  .bc-search {
    position: relative;
    padding: 10px 16px;
    flex-shrink: 0;
    z-index: 1;
  }

  .bc-search::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 24px;
    transform: translateY(100%);
    background: linear-gradient(to bottom, #ffffff, transparent);
    pointer-events: none;
  }

  .bc-search__icon {
    position: absolute;
    left: 29px;
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
  }

  .bc-search input {
    width: 100%;
    height: 42px;
    border: 1.5px solid #eaeaea;
    border-radius: 6px;
    padding: 0 12px 0 38px;
    font-family: inherit;
    font-size: 16px;
    color: #030712;
    letter-spacing: -0.02em;
    outline: none;
    box-sizing: border-box;
    background: white;
  }

  .bc-search input::placeholder {
    color: #6b7280;
  }

  .bc-search input:focus {
    border-color: #d1d5db;
  }

  /* ── Bank list ────────────────────────────── */
  .bc-list {
    flex: 1;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: #eaeaea transparent;
    padding-bottom: max(0px, 908px - 100vh);
  }

  .bc-list::-webkit-scrollbar {
    width: 4px;
  }
  .bc-list::-webkit-scrollbar-thumb {
    background: #eaeaea;
    border-radius: 2px;
  }

  /* ── Home Indicator ─────────────────────────────── */

  .bc-indicator {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    bottom: 0;
    width: 120px;
    margin-bottom: 8px;
    height: 4.8px;
    background: #000;
    border-radius: 9999px;
  }

  /* ── Bank row ─────────────────────────────── */
  .bc-row {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 12px;
    padding: 10px 16px;
    position: relative;
    flex-shrink: 0;
    cursor: pointer;
    transition: background 0.15s ease;
  }

  .bc-row:not(.bc-row--skeleton):hover {
    background: #f9fafb;
  }

  /* Divider starts after logo: 16px pad + 32px logo + 12px gap = 60px */
  .bc-row:not(:last-child)::after {
    content: "";
    position: absolute;
    left: 60px;
    right: 0;
    bottom: 0;
    height: 1px;
    background: #eaeaea;
  }

  /* ── Logo Container ──────────────────────────── */
  .bc-logo {
    width: 32px;
    height: 32px;
    flex-shrink: 0;
    overflow: hidden;
  }

  .bc-logo img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
    border: 1px solid #e6e7ea;
    box-sizing: border-box;
  }

  [data-style="circle"] .bc-logo img {
    border-radius: 50%;
  }

  [data-style="square"] .bc-logo img {
    border-radius: 4px;
  }

  /* ── Row name ─────────────────────────────── */
  .bc-row__name {
    flex: 1;
    min-width: 0;
    font-size: 16px;
    font-weight: 400;
    color: #030712;
    letter-spacing: -0.02em;
    line-height: 20px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* ── Empty state ──────────────────────────── */
  .bc-empty {
    padding: 32px 16px;
    text-align: center;
    font-size: 14px;
    color: #6b7280;
  }

  /* ── Skeleton ─────────────────────────────── */
  @keyframes bc-shimmer {
    from {
      background-position: -200% 0;
    }
    to {
      background-position: 200% 0;
    }
  }

  .bc-skel {
    background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
    background-size: 200% 100%;
    animation: bc-shimmer 1.4s ease-in-out infinite;
    border-radius: 4px;
  }

  .bc-row--skeleton .bc-logo.bc-skel {
    border-radius: 50%;
  }

  .bc-skel--name {
    flex: 1;
    height: 14px;
    max-width: 55%;
  }
</style>
